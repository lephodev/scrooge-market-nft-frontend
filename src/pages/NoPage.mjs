const NoPage = () => {
  return (
    <h1
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      404
    </h1>
  );
};

export default NoPage;
